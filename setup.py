from setuptools import setup

setup(
    name='diff_stral',
    python_requires='>=3.6<3.10',
    version='0.0.1',
    install_requires=[
        'matplotlib',
        'mpl_toolkits',
        'numpy',
        'torch',
        'yacs',
    ],
)
